@model ShanDistribution.Models.Product
@{
    ViewData["Title"] = "Delete Product";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow-sm border-0">
                <div class="card-header bg-success text-white d-flex justify-content-between align-items-center">
                    <h4 class="mb-0">Delete Product</h4>
                    <a asp-action="Index" class="btn btn-outline-light btn-sm">Back to List</a>
                </div>
                <div class="card-body p-4">
                    <h5 class="text-danger">Are you sure you want to delete this product?</h5>
                    <p class="text-muted">This action cannot be undone.</p>
                    <hr />

                    <dl class="row">
                        <dt class="col-sm-4">@Html.DisplayNameFor(m => m.ProductName)</dt>
                        <dd class="col-sm-8 fw-bold">@Model.ProductName</dd>

                        <dt class="col-sm-4">@Html.DisplayNameFor(m => m.Price)</dt>
                        <dd class="col-sm-8 fw-bold text-success">@Model.Price.ToString("C")</dd>
                    </dl>

                    <form asp-action="Delete" method="post" class="d-flex justify-content-between mt-4">
                        <input type="hidden" asp-for="ProductId" />
                        <a asp-action="Index" class="btn btn-secondary">Cancel</a>
                        <button type="submit" class="btn btn-danger px-4">Delete</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>